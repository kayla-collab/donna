{
  "name": "clarity-webapp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:worker": "wrangler deploy",
    "dev:worker": "wrangler dev",
    "deploy:pages": "wrangler pages deploy .",
    "deploy": "npm run deploy:pages"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
