{
  "name": "clarity-webapp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:worker": "wrangler pages deploy .",
    "dev:worker": "wrangler pages dev .",
    "deploy:pages": "wrangler pages deploy .",
    "deploy": "echo 'No build step required - Cloudflare Pages automatically deploys static assets and functions'",
    "build": "echo 'No build step required'"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251229.0",
    "wrangler": "^3.78.0"
  }
}
